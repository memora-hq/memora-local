{
  "name": "memora-local-repo",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm --filter @smritheon/memora-local --filter @smritheon/memora-local-cli run build",
    "test": "pnpm -r run test",
    "typecheck:vscode-extension": "pnpm --filter memora-local run build",
    "desktop:build": "pnpm run build && pnpm --filter @memora/desktop run build",
    "check:private-boundary": "node scripts/check-private-boundary.mjs"
  },
  "packageManager": "pnpm@9.0.0"
}
